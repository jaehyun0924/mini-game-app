{
  "name": "mini-game-app-firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "firestore.rules 보안 규칙 단위 테스트 (Firebase 로컬 에뮬레이터 사용)",
  "scripts": {
    "test": "firebase emulators:exec --config ../firebase.json --project=demo-mini-game-app --only firestore \"mocha --timeout 20000 *.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "mocha": "^10.7.3"
  }
}
